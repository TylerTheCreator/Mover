import { noBids } from 'mover/helpers/no-bids';
import { module, test } from 'qunit';

module('Unit | Helper | no bids');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = noBids([42]);
  assert.ok(result);
});
