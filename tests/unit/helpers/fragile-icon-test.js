import { fragileIcon } from 'mover/helpers/fragile-icon';
import { module, test } from 'qunit';

module('Unit | Helper | fragile icon');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = fragileIcon([42]);
  assert.ok(result);
});
