import { assemblyIcon } from 'mover/helpers/assembly-icon';
import { module, test } from 'qunit';

module('Unit | Helper | assembly icon');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = assemblyIcon([42]);
  assert.ok(result);
});
