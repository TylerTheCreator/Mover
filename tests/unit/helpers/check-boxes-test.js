import { checkBoxes } from 'mover/helpers/check-boxes';
import { module, test } from 'qunit';

module('Unit | Helper | check boxes');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = checkBoxes([42]);
  assert.ok(result);
});
