import { checkAuthentication } from 'mover/helpers/check-authentication';
import { module, test } from 'qunit';

module('Unit | Helper | check authentication');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = checkAuthentication([42]);
  assert.ok(result);
});
