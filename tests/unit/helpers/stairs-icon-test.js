import { stairsIcon } from 'mover/helpers/stairs-icon';
import { module, test } from 'qunit';

module('Unit | Helper | stairs icon');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = stairsIcon([42]);
  assert.ok(result);
});
