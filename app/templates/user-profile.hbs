{{#if session.isAuthenticated}}
  Avatar:
  <img src='{{session.currentUser.profileImageURL}}' alt='avatar img'>
  <br>
  {{#link-to 'move-detail' move.id}}
    Email-Address: {{session.currentUser.email}}
  {{/link-to}}
  <br>
{{/if}}

{{user-profile-info user=model}}

<br><br><hr>
{{#if session.isAuthenticated}}
  {{#each model.moves as |move|}}
    {{move-tile move=move}}
  {{/each}}
{{/if}}
